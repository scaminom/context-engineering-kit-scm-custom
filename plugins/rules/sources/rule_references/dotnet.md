
## Skill reference

When implementing non-trivial .NET features (minimal APIs, CQRS, EF Core query design, auth, caching, cloud-native patterns), consider invoking the `dotnet-expert` skill. **Ask the user first whether to invoke this skill** — they may prefer to code without it for simple changes.
