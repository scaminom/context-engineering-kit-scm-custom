
## Skill reference

When designing or building non-trivial UI (new pages, component systems, polished interactions), consider invoking the `frontend-design` skill. **Ask the user first whether to invoke this skill** — quick tweaks and bug fixes usually don't need it.
