
## Skill reference

When designing API surfaces (new endpoints, pagination schemes, error contracts, versioning strategy), consider invoking the `api-design` skill. **Ask the user first whether to invoke this skill** — straightforward CRUD additions usually don't need it.
