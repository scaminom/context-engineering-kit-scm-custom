{
  "name": "rules",
  "version": "0.4.0",
  "description": "Project-scoped coding rules. Commands emit .claude/rules/<topic>.md with stack-aware `paths:` frontmatter; Claude Code auto-loads them per the official memory docs (no CLAUDE.md pointer needed). Rules referencing skills prompt the user before embedding the reference.",
  "author": {
    "name": "scaminom",
    "email": "sebastian.camino@proconty.com"
  }
}
