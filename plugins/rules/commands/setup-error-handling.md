---
description: Install "Typed error handling" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup Typed error handling Rule

Install `.claude/rules/error-handling.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `error-handling`
- `<rule description>` (for CLAUDE.md pointer) = `Typed error handling`
- Skill reference: **none** (skip step 5)
