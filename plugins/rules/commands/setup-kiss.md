---
description: Install "KISS principle" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup KISS principle Rule

Install `.claude/rules/kiss.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `kiss`
- `<rule description>` (for CLAUDE.md pointer) = `KISS principle`
- Skill reference: **none** (skip step 5)
