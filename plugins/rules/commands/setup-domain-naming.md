---
description: Install "Domain-specific naming" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup Domain-specific naming Rule

Install `.claude/rules/domain-specific-naming.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `domain-specific-naming`
- `<rule description>` (for CLAUDE.md pointer) = `Domain-specific naming`
- Skill reference: **none** (skip step 5)
