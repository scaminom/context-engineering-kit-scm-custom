---
description: Install "Separation of concerns" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup Separation of concerns Rule

Install `.claude/rules/separation-of-concerns.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `separation-of-concerns`
- `<rule description>` (for CLAUDE.md pointer) = `Separation of concerns`
- Skill reference: **none** (skip step 5)
