---
description: Install SOLID principles as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup SOLID Rule

Install `.claude/rules/solid.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `solid`
- `<rule description>` (for CLAUDE.md pointer) = `SOLID principles`
- Skill reference: **none** (skip step 5)
