---
description: Install "Command-Query Separation (CQS)" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup Command-Query Separation (CQS) Rule

Install `.claude/rules/command-query-separation.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `command-query-separation`
- `<rule description>` (for CLAUDE.md pointer) = `Command-Query Separation (CQS)`
- Skill reference: **none** (skip step 5)
