---
description: Install "Functional core, imperative shell" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup Functional core, imperative shell Rule

Install `.claude/rules/functional-core-imperative-shell.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `functional-core-imperative-shell`
- `<rule description>` (for CLAUDE.md pointer) = `Functional core, imperative shell`
- Skill reference: **none** (skip step 5)
