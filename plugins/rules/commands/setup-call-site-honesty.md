---
description: Install "Call-site honesty for logging" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup Call-site honesty for logging Rule

Install `.claude/rules/call-site-honesty.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `call-site-honesty`
- `<rule description>` (for CLAUDE.md pointer) = `Call-site honesty for logging`
- Skill reference: **none** (skip step 5)
