---
description: Install "Explicit side effects" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup Explicit side effects Rule

Install `.claude/rules/explicit-side-effects.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `explicit-side-effects`
- `<rule description>` (for CLAUDE.md pointer) = `Explicit side effects`
- Skill reference: **none** (skip step 5)
