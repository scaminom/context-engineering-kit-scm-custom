---
description: Install "Principle of least astonishment" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup Principle of least astonishment Rule

Install `.claude/rules/principle-of-least-astonishment.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `principle-of-least-astonishment`
- `<rule description>` (for CLAUDE.md pointer) = `Principle of least astonishment`
- Skill reference: **none** (skip step 5)
