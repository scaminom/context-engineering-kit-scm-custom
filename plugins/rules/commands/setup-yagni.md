---
description: Install "YAGNI" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup YAGNI Rule

Install `.claude/rules/yagni.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `yagni`
- `<rule description>` (for CLAUDE.md pointer) = `YAGNI`
- Skill reference: **none** (skip step 5)
