---
description: Install "Function and file size limits" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup Function and file size limits Rule

Install `.claude/rules/function-file-size-limits.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `function-file-size-limits`
- `<rule description>` (for CLAUDE.md pointer) = `Function and file size limits`
- Skill reference: **none** (skip step 5)
