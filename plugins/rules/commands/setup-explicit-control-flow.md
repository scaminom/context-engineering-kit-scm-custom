---
description: Install "Explicit control flow" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup Explicit control flow Rule

Install `.claude/rules/explicit-control-flow.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `explicit-control-flow`
- `<rule description>` (for CLAUDE.md pointer) = `Explicit control flow`
- Skill reference: **none** (skip step 5)
