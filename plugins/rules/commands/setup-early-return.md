---
description: Install "Early return pattern" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup Early return pattern Rule

Install `.claude/rules/early-return-pattern.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `early-return-pattern`
- `<rule description>` (for CLAUDE.md pointer) = `Early return pattern`
- Skill reference: **none** (skip step 5)
