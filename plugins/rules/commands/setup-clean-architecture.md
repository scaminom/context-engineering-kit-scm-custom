---
description: Install "Clean Architecture / DDD" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup Clean Architecture / DDD Rule

Install `.claude/rules/clean-architecture-ddd.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `clean-architecture-ddd`
- `<rule description>` (for CLAUDE.md pointer) = `Clean Architecture / DDD`
- Skill reference: **none** (skip step 5)
