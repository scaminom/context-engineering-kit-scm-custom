---
description: Install "DRY principle" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup DRY principle Rule

Install `.claude/rules/dry.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `dry`
- `<rule description>` (for CLAUDE.md pointer) = `DRY principle`
- Skill reference: **none** (skip step 5)
