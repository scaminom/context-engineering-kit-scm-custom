---
description: Install "Library-first approach" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup Library-first approach Rule

Install `.claude/rules/library-first-approach.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `library-first-approach`
- `<rule description>` (for CLAUDE.md pointer) = `Library-first approach`
- Skill reference: **none** (skip step 5)
