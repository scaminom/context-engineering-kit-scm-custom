---
description: Install "Explicit data flow" as a project rule scoped to the detected stack's source files
argument-hint: No arguments
---

# Setup Explicit data flow Rule

Install `.claude/rules/explicit-data-flow.md` using the shared routine.

Read `$MARKETPLACE/plugins/rules/STACK_DETECTION.md` and follow its **Standard command flow** with:

- `<slug>` = `explicit-data-flow`
- `<rule description>` (for CLAUDE.md pointer) = `Explicit data flow`
- Skill reference: **none** (skip step 5)
